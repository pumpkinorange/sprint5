Задание 2. Заливка таблицы ranks
Реализуйте DAG, состоящий из одного шага. Он должен вычитывать полное состояние таблицы ranks в базе-источнике и складывать содержимое в таблицу stg.bonussystem_ranks в базе DWH.
Настройте DAG на выполнение раз в 15 минут.