Какая витрина нужна
В предыдущей теме вы уже спроектировали структуру целевой витрины dm_settlement_report. В неё входят такие поля:
id — идентификатор записи;
restaurant_id — ID ресторана, которому компания перечисляет необходимые суммы;
restaurant_name — наименование ресторана;
settlement_date — дата отчёта;
orders_count — количество заказов за период (месяц);
orders_total_sum — общая стоимость заказов;
orders_bonus_payment_sum — сумма оплат бонусами за период;
orders_bonus_granted_sum — сумма накопленных бонусов за период;
order_processing_fee — сумма, удержанная компанией за обработку заказов, которая высчитывается как orders_total_sum * 0.25;
restaurant_reward_sum — сумма, которую необходимо перечислить ресторану, высчитываемая как total_sum - orders_bonus_payment_sum - order_processing_fee.
Для расчёта показателей стоит отбирать только заказы с финальным статусом CLOSED.
Реализация DAG
Заказчик ждёт готовую, регулярно обновляемую витрину. Все необходимые для неё данные уже лежат в слое DDS. Напишите DAG, который реализует наполнение витрины.
Задание 1. Заполните таблицу dm_settlement_report
Создайте шаблон DAG и реализуйте логику заполнения витрины.

Подсказка:
Напишите логику расчёта витрины. Учитывайте два момента:
DAG запускается регулярно — ваша логика должна корректно отрабатывать при повторных запусках.
Данные в слое DDS постоянно добавляются.